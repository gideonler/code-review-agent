name: Sentinel PR Review
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  pull-requests: write
  contents: read
jobs:
  review:
    uses: gideonler/sentinel-bot/.github/workflows/pr-review.yml@main
    secrets: inherit
