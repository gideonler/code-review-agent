# All heavy dependencies (anthropic, gitpython, etc.) come from the agent package.
# Install the agent first: pip install -e ./code-review-agent
# This file exists for any sentinel-bot-specific additions in future.
